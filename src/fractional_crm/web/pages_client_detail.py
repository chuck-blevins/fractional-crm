"""Client detail page: profile + interactions timeline + log-interaction form (CRB-32).

STUB — not yet implemented. Specified by specs/task-crb32.md and pinned by
tests/web/test_interactions_ui.py. Empty router so the app builds and only the CRB-32
tests are red.
"""
from fastapi import APIRouter

router = APIRouter(prefix="/clients")
